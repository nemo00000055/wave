import { $, $$ } from './dom.js';import { HEROES, CREATURES } from '../systems/constants.js';export function renderAll(state){const hs=$('#select-hero'),cs=$('#select-creature');if(hs&&hs.options.length===0)hs.innerHTML=HEROES.map(h=>`<option value="${h.id}">${h.name}</option>`).join('');if(cs&&cs.options.length===0)cs.innerHTML=CREATURES.map(c=>`<option value="${c.id}">${c.name}</option>`).join('');const el=$('#next-wave-preview');if(el)el.textContent=state?.meta?`Wave ${state.meta.wave} — Theme: ${state.meta.theme||'—'}`:'—';}